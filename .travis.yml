
# Únicas ramas que ejecutan test
branches:
  only:
  - master

services:
  - postgresql

language: python

python:
  - 2.7

install:
  - pip install -r requirements.txt

script:
  - python manage.py test
